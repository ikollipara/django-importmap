[project]
name = "django-importmap"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.1.1",
    "httpx>=0.27.2",
]

[tool.uv]
dev-dependencies = [
    "coverage>=7.6.1",
]
